using namespace std;
#include <bits/stdc++.h>
int n, k;
vector<int> a[1000];
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int t;
        cin >> t;
        while (t--)
        {
            cin >> n >> k;
        }
    }
}