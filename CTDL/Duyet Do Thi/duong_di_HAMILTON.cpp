using namespace std;
#include <bits/stdc++.h>
int a[100][100], n, k, chuaxet[1000], b[1000];
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        cin >> n >> k;
        for (int i = 1; i <= k; i++)
        {
            int x, y;
            cin >> x >> y;
            a[x][y] = 1;
        }
        chuaxet[]
    }
}