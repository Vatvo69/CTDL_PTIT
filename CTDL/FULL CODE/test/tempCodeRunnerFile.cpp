for (int i = n - 1; i >= 0; i--)
        // {
        //     while (!st.empty() || a[st.top()] >= a[i])
        //         st.pop();
        //     if (st.empty())
        //         dp[i] = -1;
        //     else
        //         dp[i] = st.top();
        //     st.push(i);
        // }
        // for (int i = 0; i < n; i++)
        //     cout << dp[i] << " ";
        // cout << endl;